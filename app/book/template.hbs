<ul class="side-nav">
	<li class="padd">{{input value=recipeFilter placeholder="filter by keyword"}}</li>
	{{#each recipe in filteredRecipes}}
		{{#unless recipe.deleted}}
	  <li>{{#link-to "recipe" recipe}}{{recipe.name}}{{/link-to}}</li>
		{{/unless}}
	{{/each}}
</ul>

<div class="large-8 columns">
	{{outlet}}
</div>
